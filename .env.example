# Redis (required for M2)
REDIS_URL=redis://localhost:6379/0

# Optional: send high-urgency alerts (S > 0.8) to Slack. Get URL from api.slack.com/apps -> Incoming Webhooks.
# Copy your webhook URL from .env or add it here (uncomment and set). Do not commit real URLs to git.
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../xxx
